// Modelo do config.js — mostra quais valores o app espera encontrar em
// window.SUPABASE_URL / window.SUPABASE_KEY.
// Pegue os valores reais em: Supabase Dashboard → Project Settings → API.
//
// Neste projeto o config.js real também fica versionado no Git (o deploy é
// direto deste repositório, sem build) — este arquivo .example serve só de
// referência/documentação para quem for recriar o projeto do zero.

window.SUPABASE_URL = 'https://SEU_PROJETO.supabase.co';
window.SUPABASE_KEY = 'sua_chave_publishable_aqui';
